# CodePilot 技术文档

## 项目概述

**CodePilot** 是 Claude Code 的原生桌面 GUI 客户端，基于 Electron + Next.js 构建的跨平台应用。它通过 Anthropic 官方提供的 Claude Agent SDK 与 Claude Code CLI 进行交互，为用户提供图形化的代码辅助体验。

- **项目名称**: codepilot
- **当前版本**: 0.10.11
- **作者**: op7418
- **技术架构**: Electron + Next.js (App Router)

---

## 目录

1. [项目结构](#1-项目结构)
2. [技术栈](#2-技术栈)
3. [核心功能模块](#3-核心功能模块)
4. [数据库设计](#4-数据库设计)
5. [API 接口设计](#5-api-接口设计)
6. [Claude Agent SDK 集成](#6-claude-agent-sdk-集成)
7. [IPC 通信机制](#7-ipc-通信机制)
8. [构建与打包](#8-构建与打包)
9. [配置管理](#9-配置管理)
10. [改造建议 (CodeAnywhere)](#10-改造建议-codeanywhere)

---

## 1. 项目结构

```
codepilot/
├── electron/                    # Electron 主进程
│   ├── main.ts                  # 主进程入口（窗口管理、服务器启动）
│   └── preload.ts               # 预加载脚本（IPC 桥接）
│
├── src/                         # Next.js 应用源码
│   ├── app/                     # App Router 路由
│   │   ├── api/                 # API 路由
│   │   │   ├── chat/            # 聊天 API
│   │   │   ├── files/           # 文件操作 API
│   │   │   ├── plugins/         # 插件管理 API
│   │   │   ├── providers/       # API 提供商管理
│   │   │   ├── settings/        # 设置 API
│   │   │   ├── skills/          # 技能 API
│   │   │   └── tasks/           # 任务 API
│   │   ├── chat/                # 聊天页面
│   │   ├── settings/            # 设置页面
│   │   ├── plugins/             # 插件页面
│   │   ├── extensions/          # 扩展页面
│   │   ├── layout.tsx           # 根布局
│   │   └── page.tsx             # 首页
│   │
│   ├── components/              # React 组件
│   │   ├── chat/                # 聊天相关组件
│   │   ├── common/              # 通用组件
│   │   ├── layout/              # 布局组件
│   │   ├── settings/            # 设置相关组件
│   │   └── ui/                  # UI 基础组件
│   │
│   ├── hooks/                   # 自定义 React Hooks
│   ├── lib/                     # 核心业务逻辑库
│   │   ├── db.ts                # 数据库操作
│   │   ├── claude-client.ts     # Claude SDK 客户端
│   │   ├── files.ts             # 文件操作
│   │   ├── permission-registry.ts # 权限管理
│   │   └── platform.ts          # 平台检测
│   │
│   └── types/                   # TypeScript 类型定义
│       └── index.ts
│
├── scripts/                     # 构建脚本
│   ├── build-electron.mjs       # Electron 构建脚本
│   └── after-pack.js            # 打包后处理（原生模块重编译）
│
├── build/                       # 构建资源（图标等）
├── public/                      # 静态资源
├── electron-builder.yml         # Electron Builder 配置
└── package.json
```

---

## 2. 技术栈

### 2.1 核心框架

| 技术 | 版本 | 用途 |
|------|------|------|
| **Electron** | 40.2.1 | 桌面应用框架 |
| **Next.js** | 16.1.6 | 前端框架（App Router） |
| **React** | 19.2.3 | UI 库 |
| **TypeScript** | 5.x | 类型支持 |
| **Tailwind CSS** | 4.x | 样式框架 |

### 2.2 状态管理

- **React Hooks**: 本地状态管理
- **Context API**: 跨组件状态共享（PanelContext、UpdateContext）
- **localStorage**: 持久化配置（面板宽度等）

### 2.3 UI 组件

| 库 | 用途 |
|-----|------|
| **Radix UI** | 无障碍基础组件 |
| **@hugeicons/react** | 图标库 |
| **lucide-react** | 图标库补充 |
| **motion** | 动画库 |
| **cmdk** | 命令面板组件 |
| **class-variance-authority** | 组件样式变体管理 |

### 2.4 数据存储

| 技术 | 版本 | 用途 |
|------|------|------|
| **better-sqlite3** | 12.6.2 | 本地 SQLite 数据库 |
| **SQLite WAL** | - | Write-Ahead Logging 模式 |

### 2.5 Claude 集成

| 库 | 版本 | 用途 |
|-----|------|------|
| **@anthropic-ai/claude-agent-sdk** | 0.2.33 | Claude Code SDK |

### 2.6 Markdown & 代码高亮

| 库 | 用途 |
|-----|------|
| **react-markdown** | Markdown 渲染 |
| **remark-gfm** | GitHub Flavored Markdown |
| **rehype-raw** | HTML 支持 |
| **shiki** | 语法高亮引擎 |
| **react-syntax-highlighter** | 代码高亮 |
| **streamdown** | Markdown 流式处理扩展（CJK、数学公式、Mermaid） |

### 2.7 开发工具

| 工具 | 用途 |
|------|------|
| **electron-builder** | 应用打包 |
| **esbuild** | Electron 代码编译 |
| **@playwright/test** | E2E 测试 |
| **concurrently** | 并行执行脚本 |
| **wait-on** | 等待服务就绪 |

---

## 3. 核心功能模块

### 3.1 主进程 (electron/main.ts)

主进程是 Electron 应用的核心，负责：

#### 窗口管理
```typescript
// 窗口配置
{
  width: 1280,
  height: 860,
  minWidth: 800,
  minHeight: 600,
  titleBarStyle: 'hiddenInset' | 'hidden',  // macOS / Windows
}
```

#### 服务器进程管理
- **开发模式**: 连接到 `http://localhost:3000`（Next.js 开发服务器）
- **生产模式**: 启动内置的 Next.js standalone 服务器
- 使用 `utilityProcess.fork()` 运行服务器进程（避免 macOS Dock 图标）

#### 环境变量加载
```typescript
function loadUserShellEnv(): Record<string, string> {
  // 从用户 shell（.zshrc/.bashrc）加载完整环境变量
  // 解决从 Dock/Finder 启动时环境变量不完整的问题
}
```

#### 安装向导
- 检测 Node.js 和 Claude Code CLI 是否已安装
- 支持自动安装 Node.js（通过 Homebrew/winget）
- 自动安装 `@anthropic-ai/claude-code` npm 包

#### 原生模块 ABI 检查
```typescript
function checkNativeModuleABI(): void {
  // 验证 better-sqlite3.node 是否与 Electron ABI 兼容
  // 防止因 Node.js 版本不匹配导致的崩溃
}
```

### 3.2 渲染进程 (Next.js)

#### 页面路由
| 路由 | 功能 |
|------|------|
| `/` | 首页/重定向 |
| `/chat` | 新建聊天 |
| `/chat/[id]` | 具体会话 |
| `/settings` | 应用设置 |
| `/plugins` | 插件管理 |
| `/plugins/mcp` | MCP 服务器配置 |
| `/extensions` | 扩展管理 |

#### 核心组件
- **AppShell**: 主布局容器，管理三栏布局（侧边栏、主内容、右侧面板）
- **ChatView**: 聊天视图组件
- **MessageList**: 消息列表，支持虚拟滚动和无限加载
- **MessageInput**: 消息输入框，支持文件拖拽和粘贴
- **FileTree**: 文件树组件
- **RightPanel**: 右侧面板（文件预览、任务列表等）

### 3.3 会话管理

#### 会话模式
| 模式 | 权限行为 | 说明 |
|------|---------|------|
| `code` | `acceptEdits` | 默认模式，允许自动执行代码修改 |
| `plan` | `plan` | 计划模式，需要用户确认后再执行 |
| `ask` | `default` | 纯问答模式，禁用所有工具 |

#### 消息结构
```typescript
type MessageContentBlock =
  | { type: 'text'; text: string }
  | { type: 'tool_use'; id: string; name: string; input: unknown }
  | { type: 'tool_result'; tool_use_id: string; content: string; is_error?: boolean }
  | { type: 'code'; language: string; code: string };
```

### 3.4 文件操作

#### 文件树生成
- 递归遍历文件系统
- 支持深度限制（默认 3 层）
- 自动忽略 `node_modules`、`.git`、`dist`、`.next` 等目录

#### 文件预览
- 使用 **shiki** 进行语法高亮
- 支持行数限制（默认 200 行）
- 支持二进制文件检测

---

## 4. 数据库设计

### 4.1 数据库配置

```typescript
// 数据库位置
const dataDir = process.env.CLAUDE_GUI_DATA_DIR || '~/.codepilot';
const DB_PATH = path.join(dataDir, 'codepilot.db');

// WAL 模式启用
db.pragma('journal_mode = WAL');
db.pragma('foreign_keys = ON');
```

### 4.2 表结构

#### chat_sessions（会话表）
```sql
CREATE TABLE chat_sessions (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL DEFAULT 'New Chat',
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  model TEXT NOT NULL DEFAULT '',
  system_prompt TEXT NOT NULL DEFAULT '',
  working_directory TEXT NOT NULL DEFAULT '',
  sdk_session_id TEXT NOT NULL DEFAULT '',
  project_name TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'active',  -- 'active' | 'archived'
  mode TEXT NOT NULL DEFAULT 'code'       -- 'code' | 'plan' | 'ask'
);
```

#### messages（消息表）
```sql
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  session_id TEXT NOT NULL,
  role TEXT NOT NULL CHECK(role IN ('user', 'assistant')),
  content TEXT NOT NULL,
  created_at TEXT NOT NULL,
  token_usage TEXT,  -- JSON: { input_tokens, output_tokens, ... }
  FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE
);
```

#### tasks（任务表）
```sql
CREATE TABLE tasks (
  id TEXT PRIMARY KEY,
  session_id TEXT NOT NULL,
  title TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending',
  description TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE
);
```

#### api_providers（API 提供商表）
```sql
CREATE TABLE api_providers (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  provider_type TEXT NOT NULL DEFAULT 'anthropic',
  base_url TEXT NOT NULL DEFAULT '',
  api_key TEXT NOT NULL DEFAULT '',
  is_active INTEGER NOT NULL DEFAULT 0,
  sort_order INTEGER NOT NULL DEFAULT 0,
  extra_env TEXT NOT NULL DEFAULT '{}',
  notes TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

#### settings（设置表）
```sql
CREATE TABLE settings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT NOT NULL UNIQUE,
  value TEXT NOT NULL
);
```

### 4.3 数据库迁移

系统支持自动迁移，在 `migrateDb()` 函数中处理：
- 检测现有表结构
- 动态添加缺失的列
- 从旧位置迁移数据库文件

---

## 5. API 接口设计

### 5.1 聊天 API

#### POST /api/chat
发送消息并获取流式响应。

**请求体**:
```typescript
{
  session_id: string;
  content: string;
  model?: string;
  mode?: 'code' | 'plan' | 'ask';
  files?: FileAttachment[];
  toolTimeout?: number;
}
```

**响应**: SSE 流式事件

#### POST /api/chat/permission
响应权限请求。

**请求体**:
```typescript
{
  permissionRequestId: string;
  decision: {
    behavior: 'allow';
    updatedPermissions?: PermissionSuggestion[];
  } | {
    behavior: 'deny';
    message?: string;
  };
}
```

### 5.2 会话 API

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /api/chat/sessions | 获取所有会话 |
| POST | /api/chat/sessions | 创建新会话 |
| GET | /api/chat/sessions/[id] | 获取单个会话 |
| DELETE | /api/chat/sessions/[id] | 删除会话 |
| GET | /api/chat/sessions/[id]/messages | 获取会话消息（支持分页） |

### 5.3 文件 API

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /api/files | 获取文件树 |
| POST | /api/files/browse | 浏览目录 |
| POST | /api/files/preview | 预览文件内容 |
| GET | /api/files/raw | 获取原始文件 |

### 5.4 提供商 API

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /api/providers | 获取所有提供商 |
| POST | /api/providers | 创建提供商 |
| PUT | /api/providers/[id] | 更新提供商 |
| DELETE | /api/providers/[id] | 删除提供商 |
| POST | /api/providers/[id]/activate | 激活提供商 |

### 5.5 设置 API

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /api/settings | 获取所有设置 |
| POST | /api/settings | 保存设置 |
| GET | /api/settings/app | 获取应用配置 |

### 5.6 MCP 插件 API

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /api/plugins/mcp | 获取 MCP 配置 |
| POST | /api/plugins/mcp | 添加 MCP 服务器 |
| DELETE | /api/plugins/mcp/[name] | 删除 MCP 服务器 |

---

## 6. Claude Agent SDK 集成

### 6.1 SDK 初始化

```typescript
import { query } from '@anthropic-ai/claude-agent-sdk';

const queryOptions: Options = {
  cwd: workingDirectory || os.homedir(),
  abortController,
  includePartialMessages: true,
  permissionMode: 'acceptEdits' | 'plan' | 'bypassPermissions',
  env: sanitizeEnv(sdkEnv),
  pathToClaudeCodeExecutable: claudePath,
  model: model,
  systemPrompt: {
    type: 'preset',
    preset: 'claude_code',
    append: systemPrompt,
  },
  mcpServers: toSdkMcpConfig(mcpServers),
  resume: sdkSessionId,  // 恢复会话
};
```

### 6.2 消息类型处理

| SDK 消息类型 | 处理方式 |
|-------------|---------|
| `assistant` | 提取文本和工具调用 |
| `user` | 处理工具结果 |
| `stream_event` | 实时文本增量 |
| `system` | 会话初始化信息 |
| `tool_progress` | 工具执行进度 |
| `result` | 最终结果和 token 统计 |

### 6.3 流式响应 (SSE)

```typescript
// SSE 事件类型
type SSEEventType =
  | 'text'               // 文本增量
  | 'tool_use'           // 工具调用
  | 'tool_result'        // 工具结果
  | 'tool_output'        // 工具输出（stderr）
  | 'tool_timeout'       // 工具超时
  | 'status'             // 状态更新
  | 'result'             // 最终结果
  | 'error'              // 错误
  | 'permission_request' // 权限请求
  | 'done';              // 完成
```

### 6.4 权限管理

```typescript
queryOptions.canUseTool = async (toolName, input, opts) => {
  // 1. 发送 permission_request SSE 事件
  // 2. 等待用户响应（通过 POST /api/chat/permission）
  // 3. 返回用户决定
  return registerPendingPermission(permissionRequestId, input, opts.signal);
};
```

### 6.5 Hooks

```typescript
queryOptions.hooks = {
  Notification: [{
    hooks: [async (input) => {
      // 处理 Claude Code 通知
    }],
  }],
  PostToolUse: [{
    hooks: [async (input) => {
      // 工具执行完成后的回调
    }],
  }],
};
```

### 6.6 文件附件处理

- **图片文件**: 作为 base64 多模态内容发送（vision 支持）
- **非图片文件**: 保存到 `.codepilot-uploads/` 目录，通过 Read 工具引用

---

## 7. IPC 通信机制

### 7.1 预加载脚本 (preload.ts)

```typescript
import { contextBridge, ipcRenderer } from 'electron';

contextBridge.exposeInMainWorld('electronAPI', {
  // 安装相关
  checkPrerequisites: () => ipcRenderer.invoke('install:check-prerequisites'),
  startInstall: (options) => ipcRenderer.invoke('install:start', options),
  cancelInstall: () => ipcRenderer.invoke('install:cancel'),
  getInstallLogs: () => ipcRenderer.invoke('install:get-logs'),

  // 进度监听
  onInstallProgress: (callback) => {
    ipcRenderer.on('install:progress', (_event, state) => callback(state));
  },

  // 版本信息
  versions: {
    node: process.versions.node,
    chrome: process.versions.chrome,
    electron: process.versions.electron,
  },
});
```

### 7.2 安全设计

- **contextIsolation**: `true` - 隔离渲染进程和预加载脚本
- **nodeIntegration**: `false` - 禁用渲染进程 Node.js 集成
- **contextBridge**: 安全暴露有限 API 到渲染进程

---

## 8. 构建与打包

### 8.1 构建脚本

```json
{
  "scripts": {
    "dev": "next dev",
    "electron:dev": "concurrently -k \"next dev\" \"wait-on http://localhost:3000 && electron .\"",
    "electron:build": "next build && node scripts/build-electron.mjs",
    "electron:pack": "npm run electron:build && electron-builder --config electron-builder.yml",
    "electron:pack:mac": "npm run electron:build && electron-builder --mac",
    "electron:pack:win": "npm run electron:build && electron-builder --win",
    "electron:pack:linux": "npm run electron:build && electron-builder --linux"
  }
}
```

### 8.2 构建流程

1. **前端构建**: `next build` → 生成 standalone 输出
2. **Electron 编译**: `esbuild` 编译 TypeScript 到 `dist-electron/`
3. **原生模块处理**: `scripts/after-pack.js` 重编译 better-sqlite3
4. **应用打包**: `electron-builder` 生成平台特定安装包

### 8.3 输出格式

| 平台 | 格式 |
|------|------|
| macOS | DMG (arm64 + x64) |
| Windows | NSIS 安装包 (x64 + arm64) |
| Linux | AppImage, deb, rpm |

### 8.4 原生模块处理

```javascript
// scripts/after-pack.js
// 在打包时显式重编译 better-sqlite3 为 Electron ABI
// 确保原生模块与 Electron 内置 Node.js 版本兼容
```

---

## 9. 配置管理

### 9.1 应用数据目录

```
~/.codepilot/
├── codepilot.db          # SQLite 数据库
├── codepilot.db-wal      # WAL 日志
└── codepilot.db-shm      # 共享内存
```

### 9.2 Claude Code 配置

```
~/.claude/
├── settings.json         # Claude Code 设置
└── commands/             # 自定义命令

~/.claude.json            # MCP 服务器配置
```

### 9.3 环境变量

| 变量 | 说明 |
|------|------|
| `ANTHROPIC_API_KEY` | Anthropic API 密钥 |
| `ANTHROPIC_AUTH_TOKEN` | Anthropic 认证令牌 |
| `ANTHROPIC_BASE_URL` | API 基础 URL（用于代理） |
| `CLAUDE_GUI_DATA_DIR` | 自定义数据目录 |
| `CLAUDE_CODE_GIT_BASH_PATH` | Git Bash 路径（Windows） |

### 9.4 设置键值

```typescript
const SETTING_KEYS = {
  DEFAULT_MODEL: 'default_model',
  DEFAULT_SYSTEM_PROMPT: 'default_system_prompt',
  THEME: 'theme',
  PERMISSION_MODE: 'permission_mode',
  MAX_THINKING_TOKENS: 'max_thinking_tokens',
  DANGEROUSLY_SKIP_PERMISSIONS: 'dangerously_skip_permissions',
};
```

---

## 10. 改造建议 (CodeAnywhere)

基于对 CodePilot 源码的分析，以下是将项目改造为 **CodeAnywhere** 的建议：

### 10.1 命名修改

需要全局替换的字符串：
- `codepilot` → `codeanywhere`
- `CodePilot` → `CodeAnywhere`
- `~/.codepilot` → `~/.codeanywhere`
- `.codepilot-uploads` → `.codeanywhere-uploads`

涉及文件：
- `package.json` (name, description)
- `electron/main.ts` (数据目录、服务名)
- `electron-builder.yml` (appId, productName)
- `src/lib/db.ts` (数据库路径)
- `src/lib/claude-client.ts` (上传目录)
- `build/` 目录下的图标和资源文件

### 10.2 架构优化建议

#### 10.2.1 配置抽象
```typescript
// 建议创建 src/config/index.ts
export const APP_CONFIG = {
  name: 'codeanywhere',
  dataDir: () => process.env.CODEANYWHERE_DATA_DIR ||
    path.join(os.homedir(), '.codeanywhere'),
  uploadDir: (workDir: string) => path.join(workDir, '.codeanywhere-uploads'),
};
```

#### 10.2.2 模块化重构
```
src/
├── core/           # 核心逻辑（与业务无关）
│   ├── database/   # 数据库抽象层
│   ├── ipc/        # IPC 通信层
│   └── config/     # 配置管理
├── features/       # 功能模块
│   ├── chat/       # 聊天功能
│   ├── files/      # 文件操作
│   ├── settings/   # 设置管理
│   └── plugins/    # 插件系统
└── shared/         # 共享组件和工具
```

### 10.3 功能增强建议

#### 10.3.1 多模型支持
当前系统主要支持 Anthropic，可扩展为：
- OpenAI (GPT-4o, GPT-4 Turbo)
- Google (Gemini)
- Open Source (Ollama, LM Studio)

#### 10.3.2 云端同步
- 会话云端备份
- 跨设备设置同步
- 团队协作功能

#### 10.3.3 插件生态
- 支持自定义工具集成
- VS Code 扩展兼容
- MCP 服务器市场

### 10.4 代码质量改进

#### 10.4.1 测试覆盖
```
tests/
├── unit/           # 单元测试
├── integration/    # 集成测试
└── e2e/            # 端到端测试
```

#### 10.4.2 错误处理
- 统一错误码定义
- 全局错误边界
- 错误上报机制

#### 10.4.3 性能优化
- 消息虚拟滚动优化
- 文件树懒加载
- 数据库查询优化

### 10.5 迁移检查清单

- [ ] 修改 package.json 中的应用名称和描述
- [ ] 替换所有 `codepilot` 字符串
- [ ] 更新数据目录路径
- [ ] 替换应用图标和品牌资源
- [ ] 更新 electron-builder.yml 配置
- [ ] 修改 GitHub Actions 工作流
- [ ] 更新文档和 README
- [ ] 测试数据迁移（从旧版本导入）

---

## 附录

### A. 主要依赖版本

```json
{
  "@anthropic-ai/claude-agent-sdk": "^0.2.33",
  "better-sqlite3": "^12.6.2",
  "electron": "^40.2.1",
  "next": "16.1.6",
  "react": "19.2.3",
  "react-dom": "19.2.3",
  "tailwindcss": "^4",
  "typescript": "^5"
}
```

### B. 文件大小统计

| 类型 | 估计数量 |
|------|---------|
| TypeScript 文件 | ~50+ |
| React 组件 | ~30+ |
| API 路由 | ~20+ |
| 自定义 Hooks | ~10+ |

### C. 关键文件路径

| 文件 | 说明 |
|------|------|
| `electron/main.ts:1-780` | 主进程入口 |
| `src/lib/db.ts:1-569` | 数据库操作 |
| `src/lib/claude-client.ts:1-693` | SDK 集成 |
| `src/app/api/chat/route.ts:1-270` | 聊天 API |
| `src/types/index.ts:1-439` | 类型定义 |

---

*文档生成时间: 2026-02-18*
*基于 CodePilot v0.10.11 源码分析*
